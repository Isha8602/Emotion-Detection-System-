version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Mount a host directory to persist the SQLite database
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///data/users.db
    restart: unless-stopped